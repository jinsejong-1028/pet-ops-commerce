# Development Log - 2026-06-29

## 작업 기록 1

## 작업 브랜치

```text
chore/squash-inventory-migrations
```

## 작업 내용

- Docker DB reset을 전제로 재고 관련 migration 이력을 최종 schema 기준으로 정리했습니다.
- `V1__initial_schema.sql`에 LOT, 현재고 최종 구조를 직접 반영했습니다.
- `V2`는 location 테이블 생성 전용 migration으로 정리했습니다.
- `V4`는 stock_jobs, stock_movements 최종 구조를 직접 생성하도록 정리했습니다.
- 데이터 보정용 `V5`, cleanup 임시 테이블 방식의 `V6`, reference 보정용 `V7`을 제거했습니다.
- 모든 create table의 audit 컬럼 순서를 `created_at`, `created_by`, `updated_at`, `updated_by`, `deleted_at` 기준으로 정리했습니다.
- 고객 주문 배송정보를 `customer_order_deliveries`로 분리했습니다.
- 판매 확정 주문 `sales_orders`, 창고 출고 지시 `shipment_orders`를 추가했습니다.
- 공급사 구매 발주 `purchase_orders`, 창고 입고 지시 `receiving_orders`를 추가했습니다.
- 출고 수량 진행률은 `shipment_order_items`, 입고 확정 수량은 `receiving_order_items`에서 관리하도록 schema를 추가했습니다.

## 설계 메모

이번 작업은 운영 DB migration 방식이 아니라 포트폴리오 개발 초기의 schema 정리 작업입니다.
이미 적용된 DB에서는 checksum이 달라지므로 Docker volume reset 후 새 DB에 처음부터 적용해야 합니다.

```text
cleanup 임시 테이블 생성 금지
로컬 데이터 보정용 migration 제거
최종 schema는 create table에 직접 반영
```

## 검증

```powershell
.\gradlew.bat compileJava --console=plain
.\gradlew.bat test --console=plain
```

## Docker DB reset 필요

이번 브랜치 적용 후 로컬 DB는 기존 Flyway 이력과 맞지 않으므로 reset이 필요합니다.

```powershell
docker compose down -v
docker compose up -d
.\gradlew.bat bootRun
```
## Order Fulfillment API 구현

- `POST /api/v1/admin/sales-orders` API를 추가했습니다.
- 고객 주문을 판매 주문으로 확정하고, 출고 주문과 출고 주문 품목을 함께 생성하도록 연결했습니다.
- 판매 주문 번호 `SALES_ORDER`, 출고 주문 번호 `SHIPMENT_ORDER` 업무 번호 타입을 추가했습니다.
- 실제 재고 할당/PICK/출고 처리는 다음 단계에서 `shipment_orders` 기준으로 연결합니다.
## 판매 주문 확정 흐름 보정

- 고객 주문 생성 시 `sales_orders`와 `sales_order_items`를 `CREATED` 상태로 자동 생성하도록 보정했습니다.
- `sales_orders.order_date`를 추가해 판매 주문 업무일자를 별도 관리합니다.
- 관리자 판매 주문 API를 생성 API가 아니라 `confirm`/`cancel` API 중심으로 재정리했습니다.
- 판매 주문 확정 시 `customer_orders`도 `CONFIRMED`로 변경하고 `shipment_orders`/`shipment_order_items`를 생성합니다.
## 마무리요약 및 다음 세션 인수인계

- `feature/order-fulfillment-workflow` 브랜치에서 주문 fulfillment workflow를 구현 중입니다.
- 고객 주문 생성 시 `sales_orders`, `sales_order_items`를 `CREATED` 상태로 자동 생성하는 방향으로 보정했습니다.
- 판매 주문 확정 API는 `sales_orders`, `customer_orders`, `sales_order_items`를 확정하고 `shipment_orders`, `shipment_order_items`를 생성하는 구조입니다.
- 판매 주문 취소 API는 출고 지시 생성 전 `sales_orders`, `customer_orders`, `sales_order_items`를 취소하는 구조입니다.
- `sales_orders.order_date`는 판매 주문 업무일자 컬럼으로 추가했습니다.
- 이번 브랜치까지는 사용자가 마지막 Docker DB reset 예외를 명시했습니다.
- 이후 작업부터는 기존 migration 수정 대신 새 `V다음번호__...sql` migration을 추가합니다.
- migration에는 기존 데이터 보정용 `update/delete/임시 insert`를 넣지 않습니다.
- 이전 작업 중 `src/test/**` 파일이 수정되었으므로 다음 세션에서 유지/원복 여부를 먼저 검토합니다.
- PetOps workflow skill에 테스트 코드 수정 승인 기준과 migration/Docker reset 기준을 보강했습니다.